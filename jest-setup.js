// Jest setup file for Item Finder
// This file runs before each test suite and is the place to add
// global mocks, polyfills, or setup shared across all tests.
//
// Example: Mock expo-file-system for tests that don't need real file I/O
// jest.mock('expo-file-system', () => ({ ... }));
